node_modules/
# dist/ is intentionally committed: cerberus is distributed as a private git
# dependency (no npm publish), so consumers need the prebuilt bin on clone.
.turbo/
coverage/
*.log
*.plugin
*.tmp
.qg-tmp*
tsup.config.bundled_*.mjs
.quality-gate-attempts.json
PLAN.md
ISSUE.md
