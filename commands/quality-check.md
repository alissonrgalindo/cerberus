---
description: Check staged files against the quality gate without committing
---

Run `npx quality-gate check --staged` and show me the result.

If there are violations, for each one explain what it means and suggest a concrete fix (extract function, split component, replace `any`, add a test). Do not commit or refactor unless I ask — just report.
